# Quick Start

Use the instructions in [README.md](README.md).

The supported workflow is:

1. Start MySQL or update `backend/.env`
2. Train the ML model from the local dataset
3. Start the FastAPI backend
4. Start the Vite frontend
