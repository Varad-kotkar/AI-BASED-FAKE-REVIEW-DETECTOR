# Setup Notes

Setup instructions have been consolidated into [README.md](README.md).

Key paths:

- Dataset: `dataset/fake reviews dataset.csv`
- Backend env: `backend/.env`
- Saved model: `backend/models/model.pkl`
- Saved vectorizer: `backend/models/vectorizer.pkl`
